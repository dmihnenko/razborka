---
name: zoom-out
description: Поднимись на уровень абстракции выше и дай карту модулей/вызовов словарём проекта. Используй, когда плохо знаешь участок кода или нужно понять, как он встроен в общую картину («zoom out», «покажи картину целиком»).
---

Я плохо знаю этот участок кода. Поднимись на уровень абстракции выше. Дай карту всех релевантных модулей и их вызывающих сторон, используя доменный словарь проекта (СТО/parts, `sto_company_id`, роли, слои `types → services → hooks → components/pages`, миграции).

- Покажи поток данных: какой `services/*` дёргает какие таблицы, какой хук его оборачивает, какие страницы/компоненты используют хук.
- Отметь границы мультитенантности (`sto_company_id`) и RLS, если они проходят через этот участок.
- Никаких правок — только карта и связи. Если карту можно собрать из кода — сначала прочитай код (Grep/Glob/Explore), не угадывай.
