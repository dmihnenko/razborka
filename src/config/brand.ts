// ============================================================================
// БРЕНД — единый источник правды для названия продукта.
//
// ⚠️ ИМЯ ВРЕМЕННОЕ ("AutoRozborka"). Когда определимся с финальным названием —
//    меняем ТОЛЬКО этот файл, и оно обновляется во всех местах через
//    компонент <Logo/> и константу BRAND.
//
// Места, которые НЕ импортируют этот файл (сборочные/статические) — обновить
// вручную при переименовании:
//   • index.html       — <title> и <meta name="apple-mobile-web-app-title">
//   • vite.config.ts   — manifest.name / short_name / description (PWA)
//   • public/favicon.svg + PWA-иконки — если меняется и логотип
// ============================================================================

export const BRAND = {
  /** Полное имя/вордмарк одной строкой (шапки, копирайт, alt). */
  name: 'Razborka.net',
  /** Вордмарк-эмблема <Logo/>: крупная часть (lead) + уголок «.net» (accent). */
  wordmark: { lead: 'RAZBORKA', accent: '.net' },
  /** Подпись под логотипом (таглайн в эмблеме). */
  tagline: 'маркет · запчастей',
} as const
