# TODO — кабинет разборки (Razborka.net)

> Рабочий план по кабинету. Версионируется в git, чтобы продолжать с любой машины (ПК/ноут).
> После `git pull` — `npm install`. Перед пушем: `npm run build:check` + `npm run lint` + паритет i18n ru/uk.
> Точка отката последней сессии — тег `backup-2026-06-24-session2`.

## ✅ Сделано и отгружено (2026-06-24, коммит 3963352)

- **Импорт/экспорт авто+запчастей (XLSX):**
  - Экспорт по выбору — меню **Всё / Текущий фильтр / Выбранные** + режим выбора (чекбоксы в grid и list).
  - Кнопка **«Экспорт»** одного авто на странице авто (`exportSingleVehicleXlsx`).
  - Лист **«Сводка»** (все авто + итоги, заморозка шапки) при экспорте >1 авто.
  - Импорт: сверка **запчастей** существующих авто (цена/кол-во/статус/состояние/место) → было→стало, обновить/пропустить по каждой + досоздание новых.
  - Файлы: `src/utils/vehiclesXlsx.ts`, `src/pages/PartsVehicles.tsx`, `src/pages/PartsVehicleDetails.tsx`.
- **Аудит безопасности Э1 — ПРОЙДЕН.** RLS включена и tenant-scoped на всех parts-таблицах; RPC `get_vehicle_roi`/`get_parts_analytics` — SECURITY INVOKER (опираются на RLS, проверка членства не нужна); `execute_sql` только service_role. Дыр между тенантами нет.
- **Перф Э1:** индексы исчерпывающие; добавлен композит `(parts_company_id, created_at desc)` на orders+customers (миграция `database/migrations/2026-06-24_perf_orders_customers_created_idx.sql`, применена в прод). customers/orders — 1–2 запроса, не N+1.
- **Дашборд (ядро):** блок **«Доставка»** (НП: в пути/проблема/доставлено); **role-aware** — рядовой работник не видит выручку и суммы заказов (вместо выручки KPI «Доставка»). Файл: `src/pages/PartsDashboard.tsx`.

---

## ⬜ Осталось доделать

### 0. XLSX-экспорт — оформление таблицы (быстрый фикс)
- Колонки узкие — обрезают данные (название/OEM/категория/место не помещаются полностью). Подобрать ширины под реальный контент (или auto-fit по максимальной длине ячейки).
- Нет отступа/разделения между **Название** и **OEM-номер** — визуально сливаются. Добавить интервал (ширина/паддинг/вертикальный бордер между колонками).
- Файл: `src/utils/vehiclesXlsx.ts` — `SHEET_COLS` (ширины), `writePartsHeader`/`stylePartsBody` (бордеры/выравнивание). Включить `wrapText` где нужно, увеличить ширины «Название» и «Место хранения».

### 5b. Дашборд — период + KPI «Прибыль» (низкий риск, начать с этого)
- Переключатель периода **Сегодня / 7д / Месяц / Всё**.
- RPC `get_parts_dashboard_stats(p_company, p_rate, p_period)` — фильтр агрегатов по периоду (миграцию применяю сам через Management API).
- KPI **«Прибыль»** = выручка − себестоимость (себестоимость запчасти от цены авто/курса закупки).

### 6. Статусы заказов (средний/высокий риск — согласовать поведение)
- Расширить модель: `new → assembling → ready → shipped → delivered → completed`.
- Авторезерв запчастей при добавлении в заказ.
- Авто-`delivered` из трекинга НП (коды 9/10/11 — хелперы `shipDelivered`/`shipProblem` уже есть в `PartsDashboard.tsx`). ⚠️ Авто-смена статуса = поведенческое изменение, согласовать.
- Канбан-доска заказов + таймлайн по заказу.

### 9. Э2 — продуктивность
- Глобальный поиск **Cmd+K** (авто/запчасти/заказы/клиенты).
- Массовые операции инвентаря: переместить / задать цену / зарезервировать (`InventoryCard` уже умеет выделение `selectedIds`/`onToggleSelect`).
- Авторезерв запчасти при добавлении в заказ.

### 10. Э2 — единый UI
- Общие компоненты `<CustomerSelect>`, `<EmptyState>`, панель фильтров.
- Шкала радиусов `--cab-radius-*` в `:root` (`src/index.css`), убрать хардкод hex/px (в т.ч. в дашборде funnel/delivery: `#DC2626`/`#15803D`/`#B45309` — пока консистентно с существующим кодом).

### 11. Э3 — финальный слой
- Возвраты заказа (sold → available, вернуть в сток).
- Печать накладной / этикетки запчасти.
- История цен запчасти (триггер `log_inventory_price_change` уже есть в БД — нужна таблица истории + UI).
- a11y: aria, контраст, фокус, клавиатура.
- Микровзаимодействия.

---

## Памятка по стеку
- Миграции — применяю сам через Supabase Management API (проект `hwckvddevjucuzxdoqqh`); токен в `supabase-secrets.local` (gitignored).
- Edge Functions — деплой вручную (`supabase functions deploy`).
- Пуш — только по явному запросу; cross-machine: `git fetch` + rebase перед пушем.
- Экраны проверять вживую через owner-сессию; service_role-ключ удалять после.
- Admin-панель НЕ переводится на украинский.
